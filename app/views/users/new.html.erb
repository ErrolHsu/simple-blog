
<div class="row">
	<div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'form', f: f %>
      <%= f.submit "建立帳號", class: "btn btn-default btn-custom" %>
    <% end %>
  </div>  
</div>