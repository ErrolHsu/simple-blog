<%= render 'shared/flash' %>
<div id="gravatar_for_showpage">
  <h1><%= gravatar_for(@user, size: 120) %></h1>
</div>
<div class="row">
	<div class="col-md-9 col-xs-10 col-xs-offset-0 col-md-offset-1">

		<% if current_user == @user %>
		  <div id="icon-plus"><span class="glyphicon glyphicon-plus"></span><%= link_to "NEW ARTICLE", new_user_article_path(@user)%></div>
		<% else %>
			<div id="icon-plus"><span class="glyphicon glyphicon-book"></span><%= link_to "Blog Archive", user_articles_path(@user)%></div>
		<% end %>
		
		<% @articles.each do |article| %>
		<div class="user-article-show">
			<div class="user-article-title">
			  <%= link_to article.title, user_article_path(@user, article)%>
			  <span class="glyphicon glyphicon-tag"></span>
		  </div>
			<HR>
			<%= raw article.content.truncate(850) %></pre><br>
			<%= link_to "Read more..", user_article_path(@user, article), 
														class: "btn btn-default btn-custom" %><br>
			<div class="created-at">
				<%= article.created_at.to_s(:long_ordinal) %>
			</div>
		</div>
		<% end %>
	</div>
</div>

<div class="pagi">
<h4><%= paginate @articles, :window => 2 %><h4>
</div>
